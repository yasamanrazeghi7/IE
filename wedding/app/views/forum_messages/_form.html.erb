<%= form_for(@forum_message) do |f| %>
  <% if @forum_message.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@forum_message.errors.count, "error") %> prohibited this forum_message from being saved:</h2>

      <ul>
      <% @forum_message.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :forum_thread_id %><br>
    <%= f.number_field :forum_thread_id %>
  </div>
  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
